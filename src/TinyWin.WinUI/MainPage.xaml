<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="TinyWin.WinUI.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:TinyWin.WinUI.Models"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    Loaded="Page_Loaded">
    <Page.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}" />
            <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="16" />
        </Style>
        <Style x:Key="MutedTextStyle" TargetType="TextBlock">
            <Setter Property="Opacity" Value="0.68" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>
        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
    </Page.Resources>

    <Grid Padding="32,24,32,28" RowSpacing="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid ColumnSpacing="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Spacing="6">
                <TextBlock Text="TINYWIN  /  WORKBENCH" FontSize="12" FontWeight="SemiBold" CharacterSpacing="90" Opacity="0.62" />
                <TextBlock Text="Windows 映像精简工作台" FontSize="34" FontWeight="SemiBold" />
                <TextBlock Text="选择需要的 Entry，生成可审计、可复现的离线镜像。" Style="{StaticResource MutedTextStyle}" FontSize="15" />
            </StackPanel>
            <Border Grid.Column="1" Padding="16,11" VerticalAlignment="Center" CornerRadius="12" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <ProgressRing Width="18" Height="18" IsActive="{x:Bind ViewModel.IsBuilding, Mode=OneWay}" />
                    <StackPanel Spacing="2">
                        <TextBlock Text="当前计划" FontSize="12" Opacity="0.62" />
                        <TextBlock Text="{x:Bind ViewModel.SelectionSummary, Mode=OneWay}" FontWeight="SemiBold" />
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

        <Grid Grid.Row="1" ColumnSpacing="22">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="360" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Spacing="14">
                    <Border Padding="20" Style="{StaticResource CardStyle}">
                        <StackPanel Spacing="16">
                            <StackPanel Spacing="4">
                                <TextBlock Text="输入镜像" Style="{StaticResource SectionTitleStyle}" />
                                <TextBlock Text="从 ISO 或已挂载的安装介质开始。" Style="{StaticResource MutedTextStyle}" />
                            </StackPanel>
                            <StackPanel Spacing="8">
                                <Button Content="选择 ISO 文件…" HorizontalAlignment="Left" Click="SelectIsoButton_Click" />
                                <TextBlock Text="{x:Bind ViewModel.SourcePath, Mode=OneWay}" TextWrapping="Wrap" TextTrimming="CharacterEllipsis" Opacity="0.68" />
                            </StackPanel>
                            <StackPanel Spacing="7">
                                <ComboBox Header="安装映像" ItemsSource="{x:Bind ViewModel.Images}" SelectedItem="{x:Bind ViewModel.SelectedImage, Mode=TwoWay}" IsEnabled="{x:Bind ViewModel.HasLoadedImages, Mode=OneWay}" PlaceholderText="请先选择并读取安装 ISO">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate x:DataType="models:TinyWinImageInfo">
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="{x:Bind DisplayName}" />
                                                <TextBlock Text="{x:Bind ImageDescription}" FontSize="12" Opacity="0.62" TextWrapping="Wrap" />
                                            </StackPanel>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <ProgressRing Width="16" Height="16" IsActive="{x:Bind ViewModel.IsInspectingImage, Mode=OneWay}" />
                                    <TextBlock Text="{x:Bind ViewModel.ImageInspectionSummary, Mode=OneWay}" VerticalAlignment="Center" FontSize="12" Opacity="0.68" />
                                </StackPanel>
                            </StackPanel>
                            <ToggleSwitch Header="输出可启动 ISO" OnContent="开启" OffContent="关闭" IsOn="{x:Bind ViewModel.CreateIso, Mode=TwoWay}" />
                            <TextBlock Text="需要 Windows ADK Deployment Tools 中的 oscdimg.exe。" Style="{StaticResource MutedTextStyle}" FontSize="12" />
                        </StackPanel>
                    </Border>

                    <InfoBar
                        IsOpen="{x:Bind ViewModel.IsBuilding, Mode=OneWay}"
                        IsClosable="False"
                        Severity="Informational"
                        Title="正在构建镜像"
                        Message="PowerShell 正在处理安装映像，请勿关闭窗口。" />

                    <Border Padding="20" Style="{StaticResource CardStyle}">
                        <StackPanel Spacing="12">
                            <TextBlock Text="构建状态" Style="{StaticResource SectionTitleStyle}" />
                            <Border Padding="12" CornerRadius="10" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                                <StackPanel Orientation="Horizontal" Spacing="10">
                                    <Ellipse Width="8" Height="8" Fill="{ThemeResource AccentFillColorDefaultBrush}" VerticalAlignment="Center" />
                                    <TextBlock Text="{x:Bind ViewModel.Status, Mode=OneWay}" TextWrapping="Wrap" />
                                </StackPanel>
                            </Border>
                            <TextBlock Text="构建前会先归一化 Entry 计划，再开始修改映像。" Style="{StaticResource MutedTextStyle}" FontSize="12" />
                        </StackPanel>
                    </Border>

                    <Border Padding="20" Style="{StaticResource CardStyle}">
                        <Grid RowSpacing="10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="180" />
                            </Grid.RowDefinitions>
                            <Grid ColumnSpacing="8">
                                <TextBlock Text="执行日志" Style="{StaticResource SectionTitleStyle}" />
                                <TextBlock Grid.Column="1" Text="LIVE" VerticalAlignment="Center" FontSize="11" FontWeight="SemiBold" Opacity="0.55" />
                            </Grid>
                            <TextBox Grid.Row="1" Text="{x:Bind ViewModel.BuildLog, Mode=OneWay}" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Disabled" FontFamily="Cascadia Mono" FontSize="12" PlaceholderText="构建输出会显示在这里。" />
                        </Grid>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <Border Grid.Column="1" Padding="22" Style="{StaticResource CardStyle}">
                <Grid RowSpacing="18">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Spacing="4">
                            <TextBlock Text="选择精简项" Style="{StaticResource SectionTitleStyle}" />
                            <TextBlock Text="每个 Entry 都是独立、可审计的变更单元。" Style="{StaticResource MutedTextStyle}" />
                        </StackPanel>
                        <Button Grid.Column="1" Content="常规全选" Command="{x:Bind ViewModel.SelectAllCommand}" ToolTipService.ToolTip="只选择常规精简项；不会勾选专家或实验项。" />
                        <Button Grid.Column="2" Content="全部选择" Command="{x:Bind ViewModel.SelectAllEntriesCommand}" ToolTipService.ToolTip="选择所有条目，包括专家和实验项。" />
                        <Button Grid.Column="3" Content="重新加载" Command="{x:Bind ViewModel.RefreshEntriesCommand}" />
                    </Grid>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <ItemsControl ItemsSource="{x:Bind ViewModel.Entries}" HorizontalContentAlignment="Stretch">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="models:TinyWinEntry">
                                    <Border Margin="0,0,0,10" Padding="15,13" MinHeight="92" HorizontalAlignment="Stretch" CornerRadius="12" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                                        <Grid HorizontalAlignment="Stretch" ColumnSpacing="14">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="32" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="150" />
                                            </Grid.ColumnDefinitions>
                                            <CheckBox Width="32" MinWidth="0" IsChecked="{x:Bind IsSelected, Mode=TwoWay}" VerticalAlignment="Center" />
                                            <StackPanel Grid.Column="1" Spacing="5">
                                                <TextBlock Text="{x:Bind Title}" FontWeight="SemiBold" FontSize="15" TextWrapping="Wrap" />
                                                <TextBlock Text="{x:Bind Description}" Style="{StaticResource MutedTextStyle}" FontSize="13" TextWrapping="Wrap" />
                                                <StackPanel Orientation="Horizontal" Spacing="8">
                                                    <TextBlock Text="{x:Bind Id}" Opacity="0.48" FontFamily="Cascadia Mono" FontSize="11" />
                                                    <TextBlock Text="·" Opacity="0.35" />
                                                    <TextBlock Text="v{x:Bind Version}" Opacity="0.48" FontSize="11" />
                                                </StackPanel>
                                            </StackPanel>
                                            <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right" Spacing="7">
                                                <Border Padding="9,4" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                                                    <TextBlock Text="{x:Bind Category}" FontSize="11" Opacity="0.72" />
                                                </Border>
                                                <TextBlock Text="{x:Bind SelectionTier}" HorizontalAlignment="Right" FontSize="11" Opacity="0.65" />
                                                <TextBlock Text="{x:Bind Risk}" HorizontalAlignment="Right" FontWeight="SemiBold" FontSize="12" Opacity="0.8" />
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <Grid Grid.Row="2" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center" Spacing="2">
                            <TextBlock Text="{x:Bind ViewModel.EntryCountSummary, Mode=OneWay}" FontSize="12" Opacity="0.58" />
                            <TextBlock Text="{x:Bind ViewModel.SelectionSummary, Mode=OneWay}" FontWeight="SemiBold" />
                        </StackPanel>
                        <Button Grid.Column="1" Content="清除选择" Command="{x:Bind ViewModel.ClearSelectionCommand}" />
                        <Button Grid.Column="2" Content="开始构建" Command="{x:Bind ViewModel.BuildCommand}" Style="{StaticResource AccentButtonStyle}" />
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>
